# Normaliza line endings: tudo LF no repositório, independente do SO do dev.
# Arquivos de script Windows mantêm CRLF (cmd, bat, ps1 quebram com LF).

* text=auto eol=lf

*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf