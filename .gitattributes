# Normalize line endings: store everything as LF in the repo, regardless of OS.
# Windows script files keep CRLF (cmd, bat, ps1 break with LF).

* text=auto eol=lf

*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf